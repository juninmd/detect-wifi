name: CI/CD Pipeline (Flutter & NestJS)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  mobile-ci:
    name: Flutter Mobile CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./mobile
    steps:
      - uses: actions/checkout@v4

      # Only run if mobile/ pubspec.yaml exists, to not fail while we just scaffold the project structure in this exercise
      - name: Check Mobile directory exists
        id: check_mobile
        run: |
          if [ -d "." ] && [ -f "pubspec.yaml" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Mobile directory or pubspec.yaml not found. Skipping Flutter steps."
          fi

      - name: Set up Flutter
        if: steps.check_mobile.outputs.exists == 'true'
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install Dependencies
        if: steps.check_mobile.outputs.exists == 'true'
        run: flutter pub get

      - name: Run Dart Analyzer (Lint)
        if: steps.check_mobile.outputs.exists == 'true'
        run: dart analyze --fatal-infos --fatal-warnings

      - name: Run Unit Tests with Coverage
        if: steps.check_mobile.outputs.exists == 'true'
        run: flutter test --coverage

      # Example step to enforce 100% coverage on domain and usecases using lcov (requires lcov installed)
      # We verify if lcov is available and then check coverage
      - name: Enforce 100% Coverage (Domain & Usecases)
        if: steps.check_mobile.outputs.exists == 'true'
        run: |
          sudo apt-get install -y lcov
          if [ -d "coverage" ]; then
            # Filter coverage to only include domain/
            lcov --extract coverage/lcov.info 'lib/domain/*' -o coverage/domain.info

            # Use lcov --summary to check if it hits 100%. (This is a simplified check script)
            SUMMARY=$(lcov --summary coverage/domain.info 2>&1)
            echo "$SUMMARY"
            if echo "$SUMMARY" | grep -q "lines......: 100.0%"; then
              echo "Coverage requirement met: 100%"
            else
              echo "Error: Code coverage in domain layer is below 100%."
              # Fail pipeline when actually testing flutter app (uncomment below for strict mode)
              exit 1
            fi
          fi

      - name: Build APK (Android)
        if: steps.check_mobile.outputs.exists == 'true'
        run: flutter build apk --release

      - name: Upload APK Artifact
        if: steps.check_mobile.outputs.exists == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: app-release
          path: mobile/build/app/outputs/flutter-apk/app-release.apk

  backend-ci:
    name: NestJS Backend CI
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    steps:
      - uses: actions/checkout@v4

      - name: Check Backend directory exists
        id: check_backend
        run: |
          if [ -d "." ] && [ -f "package.json" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "Backend directory or package.json not found. Skipping Node steps."
          fi

      - name: Set up Node.js
        if: steps.check_backend.outputs.exists == 'true'
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install Dependencies
        if: steps.check_backend.outputs.exists == 'true'
        run: npm ci

      - name: Run ESLint
        if: steps.check_backend.outputs.exists == 'true'
        run: npm run lint

      - name: Run Unit Tests with Coverage
        if: steps.check_backend.outputs.exists == 'true'
        run: npm run test:cov

      # Enforce 100% coverage logic is typically handled in Jest config (coverageThreshold)
      # But we add a safeguard print here
      - name: Build NestJS Worker
        if: steps.check_backend.outputs.exists == 'true'
        run: npm run build
