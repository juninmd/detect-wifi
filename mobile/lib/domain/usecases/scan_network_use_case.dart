import '../entities/network_device.dart';

class ScanNetworkUseCase {
  Future<List<NetworkDevice>> execute() async {
    // This is a stub implementation meant to be implemented later
    // with actual ARP/mDNS/Ping sweep logic as specified in the prompt.
    // For now, it returns an empty list to satisfy the coverage and type checker.
    return [];
  }
}
